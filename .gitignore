node_modules
dist
out
.DS_Store
.eslintcache
*.log*
*.tsbuildinfo

.env
.env.local

# Override the user's global gitignore — this project commits its lockfile
# so CI (npm ci) and the actions/setup-node cache can resolve dependencies.
!package-lock.json
